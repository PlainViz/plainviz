{
  "name": "plainviz",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -C apps/playground dev",
    "build": "pnpm -r build",
    "test": "pnpm -C packages/core test",
    "render": "node --import tsx scripts/render-svg.mjs"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.2"
  }
}
